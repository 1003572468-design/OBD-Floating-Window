buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

// 删除 allprojects 块，因为已经在 settings.gradle 中配置了

task clean(type: Delete) {
    delete rootProject.buildDir
}
